apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: order-service
  namespace: {{ .Release.Namespace }}
spec:
  host: order-service
  trafficPolicy:
    loadBalancer:
      simple: ROUND_ROBIN
    outlierDetection:
      consecutive5xxErrors: {{ .Values.orderService.istio.outlierDetection.consecutive5xxErrors }}
      interval: {{ .Values.orderService.istio.outlierDetection.interval }}
      baseEjectionTime: {{ .Values.orderService.istio.outlierDetection.baseEjectionTime }}
