apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: review-service
spec:
  type: ClusterIP
  selector:
    app: review-service
  ports:
  - name: http
    port: 80
    targetPort: {{ .Values.reviewService.port }}
